Run ./deploy.sh (as root) to deploy [jitsi-meet](https://github.com/jitsi/jitsi-meet) on a new server.

Remember to edit ./jitsi-meet/config.js to use your own domain name (as the same with DOMAIN_NAME in ./deploy.sh).

